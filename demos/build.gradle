plugins {
    id 'java'
    id 'application'
}

dependencies {
    // Can depend on patcher/agent for testing
    // implementation project(':patcher')
    // implementation project(':agent')
}

application {
    mainClass = 'org.example.App'
}